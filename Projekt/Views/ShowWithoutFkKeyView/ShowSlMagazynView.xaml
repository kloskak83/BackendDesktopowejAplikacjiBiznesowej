<local:WszystkieViewBase x:Class="Projekt.Views.ShowWithoutFkKeyView.ShowSlMagazynView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt.Views.ShowWithoutFkKeyView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <!--ItemSource skąd brać dane-->
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Symbol" Binding="{Binding MagSymbol}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding MagNazwa}"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding MagOpis}"/>
                <DataGridTextColumn Header="Główny" Binding="{Binding MagGlowny}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
