<local:JedenViewModel x:Class="Projekt.Views.AddViews.AddSlGrupaKhView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt.Views.AddViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Themes/ResourceDictionary.xaml"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label  Grid.Row="0" Grid.Column="0" Content="Nazwa grupy kontrahentów"/>
        <TextBox  Grid.Row="0" Grid.Column="1" Style="{StaticResource AddDictionaryTextBox}"
      Text="{Binding Path=NazwaGrupyKontrahentow,ValidatesOnDataErrors=True,  UpdateSourceTrigger=PropertyChanged}"
                  
                  />
        
    </Grid>
</local:JedenViewModel>
